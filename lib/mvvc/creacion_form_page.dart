import 'package:flutter/widgets.dart';

import 'edicion_form_page.dart';

class CreacionFormPage extends StatelessWidget {
  const CreacionFormPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    /// TODO: mirar si no hay problema con el route wrapper de [EdicionFormPage]
    return const EdicionFormPage();
  }
}
